{
  "name": "server-testing",
  "private": true,
  "scripts": {
    "start": "NODE_DEBUG=verbose node --experimental-modules --experimental-wasm-modules ./server/server.mjs"
  },
  "dependencies": {
    "@ghost/dirname": "link:utils/dirname",
    "@ghost/dotenv": "link:utils/dotenv",
    "@ghost/logger": "link:utils/logger",
    "@ghost/memoize": "link:utils/memoize",
    "@ghost/nodewatch": "link:utils/nodewatch"
  }
}
